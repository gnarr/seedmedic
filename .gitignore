/target
/data
/staging
/.env
/config.toml
/*.db
/*.db-shm
/*.db-wal

# The built operator UI. Generated by `npm --prefix web run build`; see
# docs/todos/0021-a-react-operator-ui.md for why it is not committed. The
# tracked .gitkeep must survive, or the Rust build stops compiling.
/web/node_modules
/web/dist/*
!/web/dist/.gitkeep
/web/test-results
/web/playwright-report
